<%= form_for(server) do |f| %>
  <% if server.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(server.errors.count, "error") %> prohibited this server from being saved:</h2>

      <ul>
      <% server.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :noms %>
    <%= f.text_field :noms %>
  </div>

  <div class="field">
    <%= f.label :dirip %>
    <%= f.text_field :dirip %>
  </div>

<!--
  <div class="field">
    <%= f.label :Equipo_id %>
    <%= f.text_field :Equipo_id %>
  </div>
-->
<!--
 <%= f.collection_select(:Equipo_id, Equipo.all, :id, :nomequip) %>  
-->

<div class="control-group">
        <%= f.label :Equipo_id, class: 'control-group' %>
        <div class="controls">
        <%= collection_select( :server, :Equipo_id, Equipo.all, :id, :nomequip, {}, { :multiple => false  } ) %>
        </div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
